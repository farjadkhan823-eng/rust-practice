pub fn borrow() {
    // let s1: String = String::from("hello");
    // let len: usize = s1_length(&s1);
    // println!("s1 is {} and length is {}", s1, len);
}

// fn s1_length(s2: &String) -> usize {
//     return s2.len();
// }
